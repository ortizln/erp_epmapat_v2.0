spring:
  application:
    name: msvc-eureka
  config:
    import: configserver:http://localhost:8880
  cloud:
    config:
      enabled: true
      uri: http://localhost:8880
      import-check:
        enabled: false