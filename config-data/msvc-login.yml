server:
  port: 9091
spring:
  application:
    name: msvc-login
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.0.87:5432/ErpEpmapaT
    username: postgres
    password: 12345
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://192.168.0.87:8761/eureka