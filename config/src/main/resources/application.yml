server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        native:
          #search-locations: classpath:/config-data
          search-locations: file:../config-data

