server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/ortizln/erp_epmapat_v2.0
          search-paths: config-data
  application:
    name: config-server