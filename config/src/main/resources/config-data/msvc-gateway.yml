server:
  port: 7070
spring:
  application:
    name: msvc-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: recaudacion
          uri: http://localhost:8090
          predicates:
            Path=/cajas/**