server:
  port: 9095
eureka:
  instance:
    hostname: msvc-gestiondocumental  # El nombre del contenedor o el servicio en Docker Compose
  client:
    service-url:
      defaultZone: ${EUREKA_SERVICEURL}:8761/eureka/  # Usar el nombre del servicio Docker