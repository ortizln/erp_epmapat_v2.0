server:
  port: 9093
gateway:
  base-url: http://localhost:8080

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
