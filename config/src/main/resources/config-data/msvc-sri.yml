server:
  port: 9096
spring:
  application:
    name: msvc-sri
eureka:
  instance:
    hostname: msvc-sri  # El nombre del contenedor o el servicio en Docker Compose
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka  # Reemplazar localhost con el nombre del servicio Eureka en Docker Compose
