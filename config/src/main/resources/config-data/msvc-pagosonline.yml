server:
  port: 9094
spring:
  application:
    name: msvc-pagosonline
eureka:
  instance:
    hostname: msvc-pagosonline  # El nombre del contenedor o el servicio en Docker Compose
  client:
    service-url:
      defaultZone: http://192.168.0.70:8761/eureka  # Reemplazar localhost con el nombre del servicio Eureka en Docker Compose
app:
  api:
    base-url: 192.168.0.70:8080