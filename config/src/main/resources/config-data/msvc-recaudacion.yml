server:
  port: 9092
spring:
  application:
    name: msvc-recaudacion
eureka:
  instance:
    hostname: msvc-recaudacion  # El nombre del contenedor o el servicio en Docker Compose
  client:
    service-url:
      defaultZone: http://msvc-eureka:8761/eureka/  # Usar el nombre del servicio Docker
